<?php
    $host = "localhost";
    $user = "root";
    $pass = "";
    $db = "latihan-php";

    $koneksi = mysqli_connect($host, $user, $pass, $db);

    // if (!$koneksi) {
    //     die("koneksi gagal");
    // } else {
    //     echo "koneksi berhasil";
    // }
    // ?>